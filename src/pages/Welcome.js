import React from 'react';
import { View, Text, ScrollView } from 'react-native';

const Welcome = () => {
  return (
    <ScrollView>
      <View>
        <Text>Welcome</Text>
      </View>
    </ScrollView>
  );
}

export default Welcome;
